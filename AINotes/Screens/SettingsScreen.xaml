<pages:CustomPageContent
    x:Class="AINotes.Screens.SettingsScreen"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pages="using:AINotes.Controls.Pages">
    
    <Grid Padding="0" Margin="0" ColumnSpacing="0" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <StackPanel x:Name="SideStack" Grid.Row="0" Grid.Column="0"
                    BorderBrush="#DADCE0"
                    BorderThickness="0 0 1 0" />
        <ScrollViewer x:Name="MainScroll" Grid.Row="0" Grid.Column="1">
            <Grid x:Name="MainStackGrid" Padding="0" Margin="0" ColumnSpacing="0" RowSpacing="0" />
        </ScrollViewer>
    </Grid>
    
</pages:CustomPageContent>